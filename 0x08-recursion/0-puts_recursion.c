#include "main.h"

/**
 * _puts_recursion - function that puts a string
 * @s: character to be printed
 * @Return: Returns s
 */

void _puts_recursion(char *s)
{
	if (s == 1)
		return 1;
	else
		return s;
}

int main(void)
{
	_puts_recursion(s);
	return s;

}
