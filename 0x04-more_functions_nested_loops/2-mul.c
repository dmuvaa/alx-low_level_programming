#include<stdio.h>
#include "main.h"

/**
 * mul - multiplies two integers
 * @a and @b: integers to be multiplied
 * Return: Always 0
 */

int mul(int a, int b)
{
	int a;
	int b;

	{
		_putchar(a * b);
			return (0);
	}
}
